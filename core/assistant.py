"""
The Assistant class will orchestrate the full pipeline:

    listen (speech) -> understand (LLM) -> route (tools) -> respond (speech)

Not implemented yet. This file is a placeholder so the `core` package has
its documented entry point from Phase 0, and so other modules can already
import `core.assistant` without a ModuleNotFoundError once we start filling
it in during Phase 1.
"""

from __future__ import annotations

# Implementation begins in Phase 1.
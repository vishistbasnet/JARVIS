"""
Will define the LLMProvider abstract interface plus a concrete
AnthropicProvider implementation, so the rest of the app never talks to a
specific vendor SDK directly.

Not implemented yet — this arrives in Phase 2 (V1).
"""

from __future__ import annotations

# Implementation begins in Phase 2.